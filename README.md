# just a quick use of react-scroll with react hook

example of react-scroll is using react class, so just trying it out quickly with react hooks to see if it works with hooks. Result: It is working fine.
